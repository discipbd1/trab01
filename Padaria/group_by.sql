select nome_bairro from bairro group by nome_bairro;

select primeiro_nome from cartao group by primeiro_nome;

select nome from produto group by nome;

select descricao from tipo_pagamento group by descricao;

select nome from estado group by nome;